# Phase 5 Implementation Handoff Context

## Plan File
`c:\Users\Lenovo\.cursor\plans\phase_5_implementation_3e372e98.plan.md` — DO NOT edit this file, just reference it.

## Current Status (9 todo items)

| # | ID | Status | Description |
|---|-----|--------|-------------|
| 1 | `data-model` | **COMPLETED** | Added `access_level` (VARCHAR(20), default `'public'`) and `required_subscription_tier` (VARCHAR(20), nullable) columns to both `lessons` and `courses` tables |
| 2 | `password-reset-frontend` | **COMPLETED** | Created `/auth/forgot-password` and `/auth/reset-password` pages; added "Forgot your password?" link to login |
| 3 | `password-reset-admin` | **COMPLETED** | Implemented admin-initiated password reset via `@aws-sdk/client-cognito-identity-provider` `AdminResetUserPasswordCommand` in `super-admin-users.service.ts` |
| 4 | `backend-enforcement` | **COMPLETED** | Added access level + tier checking to `lessons.controller.ts` `GET /lessons/:id`. Created `Upora/backend/src/common/utils/subscription-tiers.ts` with `hasRequiredTier()` utility. Similar enforcement in courses controller. |
| 5 | `editor-tab-rename` | **COMPLETED** | Renamed tab labels: "Details" → "Settings", "Structure" → "Stage Details" in `lesson-editor-v2.component.ts` |
| 6 | `editor-access-section` | **IN PROGRESS — PARTIALLY DONE** | Template HTML for Access & Visibility section was added to the Settings tab but the **class properties and methods are missing** (see details below) |
| 7 | `course-ui-improve` | **PENDING** | Improve course lessons view in `lesson-builder.component.ts` with course header, access settings, and lesson reordering UI |
| 8 | `course-reorder-backend` | **COMPLETED** | `PATCH /courses/:id/lesson-order` endpoint exists in `courses.controller.ts` → `courses.service.ts` |
| 9 | `frontend-access-walls` | **PENDING** | Add access walls to `lesson-overview.component.ts` and `lesson-view.component.ts` |

---

## What's DONE in Detail

### 1. Data Model (COMPLETED)
- **Entity files updated:**
  - `Upora/backend/src/entities/lesson.entity.ts` — Added `accessLevel` and `requiredSubscriptionTier` columns
  - `Upora/backend/src/entities/course.entity.ts` — Added same columns
- **Migration created:** `Upora/backend/src/migrations/1737000000000-AddAccessControlColumns.ts`
- **Database already migrated:** The ALTER TABLE statements were run against the live `upora-postgres` container (database: `upora_dev`, user: `upora_user`)
- **Tier utility:** `Upora/backend/src/common/utils/subscription-tiers.ts` — exports `hasRequiredTier()` and `getTierLabel()`

### 2. Password Reset Frontend (COMPLETED)
- **New files created:**
  - `Upora/frontend/src/app/features/auth/forgot-password.component.ts` — Email input, calls `auth.forgotPassword(email)`, navigates to reset page on success
  - `Upora/frontend/src/app/features/auth/reset-password.component.ts` — 6-digit code + new password form, calls `auth.confirmForgotPassword()`
- **Auth service updated:** `Upora/frontend/src/app/core/services/auth.service.ts`
  - Added imports: `resetPassword`, `confirmResetPassword` from `@aws-amplify/auth`
  - Added methods: `forgotPassword(email)` and `confirmForgotPassword(email, code, newPassword)`
- **Login page updated:** `Upora/frontend/src/app/features/auth/login.component.ts` — Added "Forgot your password?" link
- **Routes updated:** `Upora/frontend/src/app/app.routes.ts` — Added `/auth/forgot-password` and `/auth/reset-password` routes

### 3. Admin Password Reset (COMPLETED)
- **Backend:** `Upora/backend/src/modules/super-admin/super-admin-users.service.ts`
  - `sendPasswordReset()` method updated to use `AdminResetUserPasswordCommand` via `@aws-sdk/client-cognito-identity-provider`
  - Package `@aws-sdk/client-cognito-identity-provider` was added to backend `package.json`
  - Uses `COGNITO_USER_POOL_ID` environment variable
- **Controller:** `POST /super-admin/users/:id/send-password-reset` already existed

### 4. Backend Enforcement (COMPLETED)
- **Lessons controller:** `Upora/backend/src/modules/lessons/lessons.controller.ts`
  - `GET /lessons/:id` checks `accessLevel` and `requiredSubscriptionTier`
  - Returns `{ ...lesson, accessDenied: true, reason, requiredTier }` instead of HTTP 403
  - Reasons: `'paid'`, `'login_required'`, `'tier_required'`
- **Update DTO:** `Upora/backend/src/modules/lessons/dto/update-lesson.dto.ts` — Added `accessLevel` and `requiredSubscriptionTier` fields
- **Courses controller:** Similar access checks exist in `Upora/backend/src/modules/courses/courses.controller.ts`

### 5. Tab Rename (COMPLETED)
- In `lesson-editor-v2.component.ts` around line 4038:
  ```
  tabs = [
    { id: 'details', label: 'Settings', icon: '📋' },
    { id: 'structure', label: 'Stage Details', icon: '🏗️' },
    ...
  ]
  ```

### 8. Course Reorder Backend (COMPLETED)
- `PATCH /courses/:id/lesson-order` endpoint in `courses.controller.ts`
- `reorderLessons(courseId, lessonIds, userId, userRole)` in `courses.service.ts`

---

## What STILL NEEDS DOING

### 6. `editor-access-section` — IN PROGRESS (needs class properties & methods)

The **template HTML** for the Access & Visibility section has been added to the Settings tab (lines ~281–309 in `lesson-editor-v2.component.ts`). It includes:
- Radio buttons for access level (`public`, `login_required`, `paid` disabled)
- Tier dropdown when `login_required` is selected
- Course access warning display

**MISSING — the class body does NOT have:**
- Property declarations: `lessonAccessLevel`, `lessonRequiredTier`, `courseAccessWarning`, `parentCourseAccessLevel`, `parentCourseTitle`
- Methods: `onAccessLevelChange(value)`, `onTierChange(value)`
- Logic to load parent course access settings (when lesson has `courseId`)
- Logic to show confirmation dialog when access differs from parent course
- Logic to include `accessLevel` and `requiredSubscriptionTier` in the save/update lesson call
- CSS styles for `.access-visibility-section`, `.access-radio`, `.tier-selector`, `.course-access-warning`, `.section-divider`

**To complete this:**
1. Add class properties around line ~4057 (after tabs definition):
   ```typescript
   lessonAccessLevel: string = 'public';
   lessonRequiredTier: string = 'free';
   courseAccessWarning: string = '';
   parentCourseAccessLevel: string = 'public';
   parentCourseTitle: string = '';
   ```
2. Initialize from lesson data in `loadLesson()` — set `lessonAccessLevel = lesson.accessLevel || 'public'` and `lessonRequiredTier = lesson.requiredSubscriptionTier || 'free'`
3. If `lesson.courseId` exists, fetch the parent course to get its access settings for comparison
4. Add `onAccessLevelChange(newValue)` method — if lesson is in a course and access differs from parent, show `confirm()` dialog; if cancelled, revert
5. Add `onTierChange(newValue)` method — similar course inheritance check
6. Include `accessLevel` and `requiredSubscriptionTier` in the lesson save payload
7. Add CSS styles for the access section (dark theme, matching existing editor styles)

### 7. `course-ui-improve` — PENDING

**File:** `Upora/frontend/src/app/features/lesson-builder/lesson-builder.component.ts`

When viewing a course's lessons (`currentView === 'course-lessons'`), add:
- **Course header section** with editable title, description, Access & Visibility controls (same pattern as lesson editor), and save button
- **Lessons list** with up/down arrow buttons for reordering
- Each lesson tile shows access level indicator (inherited vs overridden)
- "Add Lesson" button at bottom
- Use existing `PATCH /courses/:id` to save course settings
- Use existing `PATCH /courses/:id/lesson-order` to persist reordering

Also needs a frontend service method for `PATCH /courses/:id/lesson-order`. Check if `courses.service.ts` has it already:
- File: `Upora/frontend/src/app/core/services/courses.service.ts`

### 9. `frontend-access-walls` — PENDING

**Files to update:**

1. `Upora/frontend/src/app/features/lesson-overview/lesson-overview.component.ts`
   - Check `lesson.accessDenied` from API response
   - If `reason === 'login_required'`: show "Login to access this lesson" banner with login button (still show overview, just block the "Start Lesson" button)
   - If `reason === 'tier_required'`: show "Upgrade to [tier] to access this lesson"
   - If `reason === 'paid'`: show "Coming soon" message

2. `Upora/frontend/src/app/features/lesson-view/lesson-view.component.ts`
   - On load, check for `accessDenied` flag in the API response
   - If present, show an access wall overlay instead of the lesson content
   - Redirect to login or show upgrade messaging

3. Route guards: No changes needed. Access control is handled at the component/API level per-lesson.

---

## Key Files Reference

### Backend
- `Upora/backend/src/entities/lesson.entity.ts` — Lesson entity (has `accessLevel`, `requiredSubscriptionTier`)
- `Upora/backend/src/entities/course.entity.ts` — Course entity (has `accessLevel`, `requiredSubscriptionTier`)
- `Upora/backend/src/common/utils/subscription-tiers.ts` — `hasRequiredTier()`, `getTierLabel()`
- `Upora/backend/src/modules/lessons/lessons.controller.ts` — Access enforcement on GET
- `Upora/backend/src/modules/lessons/dto/update-lesson.dto.ts` — Has `accessLevel` and `requiredSubscriptionTier`
- `Upora/backend/src/modules/courses/courses.controller.ts` — Course CRUD + lesson-order endpoint
- `Upora/backend/src/modules/courses/courses.service.ts` — Course business logic
- `Upora/backend/src/modules/super-admin/super-admin-users.service.ts` — Admin password reset

### Frontend
- `Upora/frontend/src/app/features/lesson-editor/lesson-editor-v2.component.ts` — Lesson editor (tabs renamed, Access HTML in template but class props/methods MISSING)
- `Upora/frontend/src/app/features/lesson-builder/lesson-builder.component.ts` — Course/lesson management (needs course UI improvements)
- `Upora/frontend/src/app/features/lesson-overview/lesson-overview.component.ts` — Needs access wall
- `Upora/frontend/src/app/features/lesson-view/lesson-view.component.ts` — Needs access wall
- `Upora/frontend/src/app/features/auth/forgot-password.component.ts` — Done
- `Upora/frontend/src/app/features/auth/reset-password.component.ts` — Done
- `Upora/frontend/src/app/features/auth/login.component.ts` — Updated with forgot password link
- `Upora/frontend/src/app/core/services/auth.service.ts` — Has `forgotPassword()` and `confirmForgotPassword()`
- `Upora/frontend/src/app/core/services/courses.service.ts` — Frontend courses service
- `Upora/frontend/src/app/app.routes.ts` — Updated with forgot/reset routes

### Docker
- Postgres container name: `upora-postgres`
- Database: `upora_dev`, User: `upora_user`
- Migration already applied to live database

---

## Version Numbers
- Frontend: `0.8.47` (in `Upora/frontend/package.json`) — needs bumping when work resumes
- Backend: `0.8.38` (in `Upora/backend/package.json`) — needs bumping when work resumes

---

## Instructions for New Chat

Paste this in the new chat:

> Continue implementing the Phase 5 plan from `c:\Users\Lenovo\.cursor\plans\phase_5_implementation_3e372e98.plan.md`. Read `PHASE5_HANDOFF.md` for detailed context on what's done and what's remaining. DO NOT edit the plan file itself. 
>
> Three items remain:
> 1. **`editor-access-section`** (IN PROGRESS) — The template HTML exists but the class properties, methods, save logic, course inheritance warning, and CSS are missing in `lesson-editor-v2.component.ts`
> 2. **`course-ui-improve`** (PENDING) — Improve course lessons view in `lesson-builder.component.ts` with course header settings, lesson reorder arrows, access controls
> 3. **`frontend-access-walls`** (PENDING) — Add access walls to `lesson-overview.component.ts` and `lesson-view.component.ts`
>
> Mark todos as in_progress as you work through them. Bump frontend and backend version numbers when done.
