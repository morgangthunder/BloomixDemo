# Development Dockerfile for Angular + Ionic Frontend
FROM node:20-alpine

WORKDIR /app

# Copy package files
COPY package*.json ./

# Install ALL dependencies (including dev dependencies)
RUN npm install

# Copy source code
COPY . .

# Expose port
EXPOSE 8100

# Start Ionic dev server on port 8100
CMD ["npm", "start", "--", "--host", "0.0.0.0", "--port", "8100", "--poll"]

