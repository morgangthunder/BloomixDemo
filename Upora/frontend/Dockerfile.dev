# Development Dockerfile for Angular + Ionic Frontend
FROM node:20-alpine

WORKDIR /app

# Copy package files
COPY package*.json ./

# Install ALL dependencies (including dev dependencies)
RUN npm install

# Copy source code
COPY . .

# Expose port
EXPOSE 8100

# Start Ionic dev server on port 8100 with aggressive polling for Docker on Windows
CMD ["npm", "run", "start:docker"]

